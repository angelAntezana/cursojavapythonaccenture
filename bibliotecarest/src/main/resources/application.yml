spring:
    application:
        name: concesionario
    h2:
        console:
            enabled: true
            path: /h2

    datasource:
        url: jdbc:h2:mem:concesionario;DB_CLOSE_DELAY=-1
        username: sa
        password:

    jpa:
           hibernate:
            ddl-auto: update
            show-sql: true
